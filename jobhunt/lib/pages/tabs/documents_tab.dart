// ignore_for_file: camel_case_types

import 'package:flutter/material.dart';

class documents_tab extends StatelessWidget {
  const documents_tab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
